extends layout

block content
    h1.mt-3= "Club Application: " + club.name
    br
    br
    form(method="POST", action="/clubs/apply/"+club._id)
        .form-group
            label Why do you want to join this club?
            textarea.form-control(name="purpose", rows="10")

        input.btn.btn-primary(type="submit", value="Apply")